<div class="row">
	<div class= "col-md-4 col-md-offset-4">
		<%= form_for(@user,url:{action: :update}) do |f| %>

			<%= f.label :otp %>
			<%= f.text_field :otp,value: "" %>

			<%= f.submit "Submit" %>
		<% end %>
	</div>
</div>